{
  "name": "jwt-auth-backend",
  "version": "1.0.0",
  "description": "JWT Authentication Backend",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:simple": "node server.js",
    "build": "echo \"Building backend...\" && npm install --omit=dev",
    "build:dev": "npm install",
    "clean": "rm -rf node_modules package-lock.json",
    "clean:install": "npm run clean && npm install",
    "postinstall": "echo \"Backend dependencies installed successfully\"",
    "lint": "echo \"Linting not configured\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "verify": "node --version && npm --version"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.6.3",
    "nodemon": "^3.1.10"
  },
  "devDependencies": {},
  "keywords": [
    "jwt",
    "authentication",
    "express",
    "mongodb",
    "api",
    "backend",
    "nodejs"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "."
  }
}
